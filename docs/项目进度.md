# 项目进度总结

> 更新时间：2026-08-31 ｜ 最新提交：`a42b892`（main/dev 已推送 GitHub）
> 三端一体：用户网站 + 管理后台 + 微信小程序，共用一套后端 API 与 MySQL 数据

---

## 一、已完成

### 1. 工程地基（企业级底座）

| 项 | 状态 |
|----|------|
| Git 分支模型（main/dev）+ Conventional Commits | ✅ 全部推送 GitHub |
| Flyway 数据库版本化迁移 | ✅ V1~V13（老库自动 baseline，新库全自动建） |
| 集成测试 | ✅ **39 个全绿**（订单/支付/会员/售后/优惠券/评价 + 图片上传 3 + 轮播图 3 + RBAC 4） |
| Docker + docker-compose | ✅ 编写完成（本机未装 Docker，装后即可用） |
| GitHub Actions CI | ✅ 后端测试 + 三端构建 |
| 配置外部化 | ✅ 密钥全走环境变量，prod profile fail-fast |

### 2. 交易闭环（M1）

登录（短信 + 微信自动登录跨设备同账号）→ 商品（10 款果小满水果 + 真实摄影图）→ 购物车（**三端同步**）→ 下单（乐观锁防超卖）→ 支付（M1 模拟）→ 发货（运单号）→ 确认收货 → 评价。

### 3. M2 营销与服务（全部完成）

- **售后退款（坏果包赔）**：申请 → ≤50 元自动秒审 / 超额人工审核 → 退款回库存 / 驳回恢复订单状态；用户可撤销
- **优惠券**：后台建券（满减/折扣/无门槛，限领/总量/时间窗）→ 领券中心（网站+小程序）→ 结算页选券抵扣 → 取消退券；乐观扣减防超发
- **评价系统**：完成单评价（星级/内容/匿名，明细粒度防重复）→ 商品详情评分聚合（均分+条数）→ 后台回复 / 显示隐藏
- **图片上传闭环**：后端上传接口（jpg/png/webp ≤5MB，日期分目录）+ 磁盘静态映射 `/uploads/**`；网站售后弹层选图即传（≤3 张、可删可重选）、小程序 `chooseImage` + `uploadFile`（相册/拍照/压缩）、管理后台售后审核页凭证缩略图 + 大图预览

- **RBAC 权限落地**：权限点 26 个（与菜单 perm、接口映射三者对齐）+ 内置角色（超级管理员/运营专员/只读观察员）；`AdminInterceptor` 按 `PermissionRegistry` 逐点校验（未登记接口默认放行、超管跳过）；后台「系统设置 → 角色权限」支持角色增删改与账号授权；登录后返回权限点，侧边菜单按权限显隐

### 4. 三端界面

- **网站**（Vue3+Vite，:5173）：首页（**后台可配轮播**/Hero/热销榜/保障条）、详情+评价区、购物车、结算选券、订单（支付/取消/确认/售后<含凭证传图>/评价）、领券中心、我的账户/收藏
- **管理后台**（Element Plus+ECharts，:5174）：七大分组菜单（**按 RBAC 权限点显隐**）、经营看板（趋势/占比/状态环图）、营业统计+Excel 导出、商品/订单/售后（凭证图预览）/评价管理、会员卡（开卡续费/临期筛选）、营销中心（优惠券 + 轮播图：上传配图/排序/启停/删除）、**系统设置（角色权限：角色 CRUD + 权限点勾选 + 账号授权）**
- **小程序**（uni-app 果小满 UI，CLI `miniapp/` + HBuilderX `miniapp-hbx/`）：真实后端数据、**首页轮播接后台配置**（无数据回落静态图）、跨端购物车同步、TabBar 数量角标、领券/评价/售后全流程（售后可拍照传凭证）、真实 AppID 已配置

### 5. 数据库

20+ 张表（用户/商品/交易/营销/售后/评价/内容/RBAC/日志），完整设计文档见 [数据库设计.md](数据库设计.md)；金额一律分制存储；订单明细快照化。

---

## 二、当前断点

**暂无阻塞断点**。图片上传闭环与 RBAC 权限均已落地（建表/接口/页面/测试齐备），下一步等营业执照办下来再接真实微信支付。

## 三、后续路线（按优先级）

| # | 任务 | 说明 | 状态 |
|---|------|------|------|
| ✅ 1 | **图片上传闭环** | 网站售后表单传图、小程序传图、后台审核页展示凭证 | ✅ 已完成 |
| 🔴 2 | **真实微信支付** | 阻塞在用户侧：无营业执照 → 无法申请微信支付商户号 → 无 mchid/APIv3 密钥可配 | ⏸ 等营业执照 |
| 🟡 3 | M3 秒杀 | `seckill_activity` 表已建；活动管理 + Redis 预扣防超卖（**pom 无 Redis 依赖、本机未装 Redis，暂无法验证**） | ⬜ |
| ✅ 4 | **RBAC 权限落地** | 四张表已建；接口权限拦截 + 后台角色管理页 + 菜单按权限显隐 | ✅ 已完成 |
| ✅ 5 | **轮播图配置** | `banner` 表已建；后台配置 + 首页轮播展示 | ✅ 已完成 |
| 🟢 6 | 上线部署 | 服务器 + HTTPS + 小程序提审（需食品经营许可证）+ 域名备案 | ⬜ |

## 三-甲、被阻塞项与接入握手清单

**真实微信支付（路线 #2）当前完全阻塞在用户侧，非开发侧问题**：微信支付必须先用「营业执照」申请商户号，没有商户号就没有 `mchid` 与 APIv3 密钥，开发方无法任何接入。一旦用户具备资质，按以下清单交给我即可改为真实支付（其余代码我来做）：

1. 营业执照（个体工商户/企业均可，水果零售需对应经营范围）
2. 微信支付商户号 `mchid`（pay.weixin.qq.com 申请）
3. APIv3 密钥（商户平台 → API 安全 → 设置 APIv3 密钥）
4. 商户 API 证书序列号 + 证书/私钥（APIv3 回调验签与退款必需）
5. 小程序/公众号 AppID（与商户号同主体，已配置 `FRUIT_WX_APPID`）

届时改动范围：`application.yml` 注入上述密钥（走环境变量，不入库）、`PayController` 由 mock 改为 JSAPI 下单 + 回调通知 + 退款，前端支付按钮无需改动。

## 四、常用命令

```bash
# 后端（:8080）
cd backend && ../tools/apache-maven-3.9.9/bin/mvn -s settings-aliyun.xml spring-boot:run
# 测试
cd backend && ../tools/apache-maven-3.9.9/bin/mvn -s settings-aliyun.xml test

# 网站（:5173）/ 管理后台（:5174）
cd web && npm run dev          # cd admin-web && npm run dev

# 小程序 CLI（微信开发者工具导入 miniapp/dist/dev/mp-weixin）
cd miniapp && npm run dev:mp-weixin
# 小程序 H5 预览（:5175）
cd miniapp && npm run dev:h5

# 一键起后端全套（需 Docker）
docker compose up -d --build
```
