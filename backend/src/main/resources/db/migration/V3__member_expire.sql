-- =====================================================================
-- V3__member_expire.sql — 会员卡模型（模型 B）：限期会员
-- member_expire_at 为空 = 未开通限期会员（仍是注册会员）
-- 有值 = 会员卡有效期至该时间；后台可开卡/续费
-- =====================================================================

ALTER TABLE `user` ADD COLUMN member_expire_at DATETIME NULL COMMENT '会员卡有效期，NULL=未开通限期会员';
