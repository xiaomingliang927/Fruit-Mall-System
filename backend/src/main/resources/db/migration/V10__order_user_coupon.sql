-- =====================================================================
-- V10__order_user_coupon.sql — 订单记录所用优惠券（取消订单时退回）
-- =====================================================================

ALTER TABLE `order` ADD COLUMN user_coupon_id BIGINT NULL COMMENT '使用的用户优惠券ID';
